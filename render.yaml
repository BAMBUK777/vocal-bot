services:
  - type: worker
    name: vocal-bot
    env: python
    plan: free
    buildCommand: ""
    startCommand: "python bot.py"
    autoDeploy: true
    envVars:
      - key: BOT_TOKEN
        sync: false
